/**
 * Admin Access Configuration
 * Temporary password access configuration for development.
 * Replace with backend auth configuration in production.
 */
export const ADMIN_ACCESS_PASSWORD = "Thiru@1012";
